# journey-theme-build:auto — regenerate with journey-theme-build catppuccin --force
$activeBorderColor = rgb(89b4fa)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}