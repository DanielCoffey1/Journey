# journey-theme-build:auto — regenerate with journey-theme-build hackerman --force
$activeBorderColor = rgb(82FB9C)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}