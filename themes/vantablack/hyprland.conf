# journey-theme-build:auto — regenerate with journey-theme-build vantablack --force
$activeBorderColor = rgb(8d8d8d)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}