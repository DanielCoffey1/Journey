# journey-theme-build:auto — regenerate with journey-theme-build gruvbox --force
$activeBorderColor = rgb(7daea3)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}