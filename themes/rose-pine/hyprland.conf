# journey-theme-build:auto — regenerate with journey-theme-build rose-pine --force
$activeBorderColor = rgb(56949f)

general {
    col.active_border = $activeBorderColor
}

group {
    col.border_active = $activeBorderColor
}