# Journey-default window rules.
windowrule = suppress_event maximize, match:class .*

# Tag windows for default opacity (apps can opt out by removing this tag)
windowrule = tag +default-opacity, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus on, match:class ^$, match:title ^$, match:xwayland 1, match:float 1, match:fullscreen 0, match:pin 0

# Per-app rules (sourced after the tagging so apps can override)
# source = ~/.local/share/journey/default/hypr/apps/walker.conf
# source = ~/.local/share/journey/default/hypr/apps/1password.conf
# (Per-app rules will be ported in Phase 2 cleanup.)

# Apply default opacity after apps have had a chance to opt out
windowrule = opacity 0.97 0.9, match:tag default-opacity
