# JetBrains IDEs lose menu focus when the mouse drifts.
# (See basecamp/omarchy#5183 for the root cause.)
windowrule {
    name = jetbrains-focus
    no_follow_mouse = on
    match:class = ^(jetbrains-.*)$
}
